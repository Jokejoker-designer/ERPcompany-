# Kế hoạch Pull Request

Branch đề xuất: `feature/integrated-modern-local-ui`

Commit:

1. `feat(ui): add zero-build modern ERP runtime interface`
2. `chore(runtime): allow selectable static web directory`
3. `test(ui): add modern UI contract and security checks`
4. `docs: document local install and rollback`

Không merge thẳng main. Kiểm tra cả `start.bat` và `start-modern.bat` trước khi duyệt.
